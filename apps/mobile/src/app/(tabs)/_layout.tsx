import { Tabs } from 'expo-router'; import { Text, type ColorValue } from 'react-native'; import { palette, radius } from '@/constants/theme';
import { FamilyGate } from '@/components/family-gate';
const icons = { home: '⌂', transactions: '↕', reports: '◫', more: '•••' } as const;
export default function TabsLayout() { return <FamilyGate><Tabs screenOptions={({ route }) => ({ headerShown: false, tabBarActiveTintColor: palette.action, tabBarInactiveTintColor: palette.textMuted, tabBarIcon: ({ color }) => <AppTabIcon symbol={icons[route.name as keyof typeof icons] ?? '•'} color={color} />, tabBarStyle: { position: 'absolute', left: 12, right: 12, bottom: 10, height: 68, paddingTop: 8, paddingBottom: 8, backgroundColor: palette.surface, borderTopWidth: 0, borderRadius: radius.lg, elevation: 10 }, tabBarLabelStyle: { fontSize: 12, fontWeight: '700' } })}><Tabs.Screen name="home" options={{ title: 'Home' }} /><Tabs.Screen name="transactions" options={{ title: 'Activity' }} /><Tabs.Screen name="reports" options={{ title: 'Insights' }} /><Tabs.Screen name="more" options={{ title: 'More' }} /></Tabs></FamilyGate>; }

function AppTabIcon({ symbol, color }: { symbol: string; color: ColorValue }) { return <Text style={{ color, fontSize: 21, lineHeight: 24, fontWeight: '800' }}>{symbol}</Text>; }
