import { router } from 'expo-router'; import { Screen } from '@/components/ui/screen'; import { AppButton } from '@/components/ui/app-button'; import { AppText } from '@/components/ui/app-text'; import { Card } from '@/components/ui/card'; import { useActiveFamily } from '@/providers/active-family-provider';
export default function HomeScreen() { const { activeFamily } = useActiveFamily(); return <Screen title="Hello" subtitle={activeFamily?.name ?? 'Your family'} action={<AppButton label="Add" onPress={() => router.push('/transaction/new')} />}><Card><AppText variant="heading">Day 1 foundation ready</AppText><AppText muted>Shared balances and recent transactions are connected in Day 2 after the approval-secured family boundary.</AppText></Card></Screen>; }
