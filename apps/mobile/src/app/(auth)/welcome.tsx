import { StyleSheet, View } from 'react-native';
import { router } from 'expo-router';
import { Screen } from '@/components/ui/screen'; import { AppText } from '@/components/ui/app-text'; import { AppButton } from '@/components/ui/app-button'; import { Card } from '@/components/ui/card'; import { palette, spacing } from '@/constants/theme';
export default function WelcomeScreen() { return <Screen><View style={styles.hero}><View style={styles.mark}><AppText variant="title">FL</AppText></View><AppText variant="title">Money is clearer when the family shares one view.</AppText><AppText muted>Track income, spending, balances, and savings together—with approval-controlled access.</AppText></View><Card><AppText variant="heading">Start securely</AppText><AppText muted>Sign in with a passwordless email link. You can create a family or accept an invitation next.</AppText><AppButton label="Continue with email" onPress={() => router.push('/(auth)/sign-in')} /></Card></Screen>; }
const styles = StyleSheet.create({ hero: { flex: 1, justifyContent: 'center', gap: spacing.lg }, mark: { width: 64, height: 64, borderRadius: 18, backgroundColor: palette.income, alignItems: 'center', justifyContent: 'center' } });
