import { StyleSheet, View } from 'react-native';
import { router } from 'expo-router';
import { Screen } from '@/components/ui/screen'; import { AppText } from '@/components/ui/app-text'; import { AppButton } from '@/components/ui/app-button'; import { Card } from '@/components/ui/card'; import { palette, radius, spacing } from '@/constants/theme';
export default function WelcomeScreen() { return <Screen><View style={styles.hero}><View style={styles.mark}><AppText style={styles.markText}>FL</AppText></View><AppText variant="display">Family money, finally in sync.</AppText><AppText muted>Track income, spending, balances and savings together—with approval-controlled access for every family member.</AppText><View style={styles.features}><View style={styles.feature}><AppText style={styles.featureIcon}>✓</AppText><AppText>Shared, permission-aware ledger</AppText></View><View style={styles.feature}><AppText style={styles.featureIcon}>↗</AppText><AppText>Clear monthly and annual insights</AppText></View><View style={styles.feature}><AppText style={styles.featureIcon}>◎</AppText><AppText>Private access with owner approval</AppText></View></View></View><Card tone="accent"><AppText variant="heading">Ready when your family is</AppText><AppText muted>Sign in securely, create a family or accept an invitation in a few taps.</AppText><AppButton label="Get started" onPress={() => router.push('/(auth)/sign-in')} /></Card></Screen>; }
const styles = StyleSheet.create({ hero: { flex: 1, justifyContent: 'center', gap: spacing.lg, paddingVertical: spacing.xl }, mark: { width: 72, height: 72, borderRadius: radius.lg, backgroundColor: palette.navy, alignItems: 'center', justifyContent: 'center' }, markText: { color: palette.white, fontWeight: '900', fontSize: 24 }, features: { gap: spacing.md, paddingTop: spacing.sm }, feature: { flexDirection: 'row', alignItems: 'center', gap: spacing.md }, featureIcon: { color: palette.action, fontWeight: '900', fontSize: 18 } });
