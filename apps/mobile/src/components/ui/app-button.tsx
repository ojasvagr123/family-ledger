import type { ReactNode } from 'react';
import { ActivityIndicator, Pressable, StyleSheet } from 'react-native';
import { palette, radius, spacing } from '@/constants/theme';
import { AppText } from './app-text';
type Props = { label: string; onPress?: () => void; disabled?: boolean; loading?: boolean; kind?: 'primary' | 'secondary' | 'destructive'; icon?: ReactNode };
export function AppButton({ label, onPress, disabled, loading, kind = 'primary', icon }: Props) { const blocked = disabled || loading; return <Pressable accessibilityRole="button" accessibilityState={{ disabled: blocked, busy: loading }} onPress={onPress} disabled={blocked} style={({ pressed }) => [styles.base, styles[kind], blocked && styles.disabled, pressed && styles.pressed]}>{loading ? <ActivityIndicator color={kind === 'primary' ? palette.actionText : palette.text} /> : icon}<AppText style={kind === 'primary' ? styles.primaryText : kind === 'destructive' ? styles.destructiveText : undefined}>{label}</AppText></Pressable>; }
const styles = StyleSheet.create({ base: { minHeight: 50, paddingHorizontal: spacing.lg, paddingVertical: spacing.sm, borderRadius: radius.md, alignItems: 'center', justifyContent: 'center', flexDirection: 'row', gap: spacing.sm, borderWidth: 1 }, primary: { backgroundColor: palette.action, borderColor: palette.action }, secondary: { backgroundColor: palette.surface, borderColor: palette.borderStrong }, destructive: { backgroundColor: palette.danger, borderColor: '#F6B9B4' }, primaryText: { color: palette.actionText, fontWeight: '800' }, destructiveText: { color: palette.destructive, fontWeight: '800' }, disabled: { opacity: 0.45 }, pressed: { transform: [{ scale: 0.985 }], opacity: 0.9 } });
