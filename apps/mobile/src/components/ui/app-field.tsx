import { StyleSheet, TextInput, View, type TextInputProps } from 'react-native';
import { palette, radius, spacing } from '@/constants/theme';
import { AppText } from './app-text';
type Props = TextInputProps & { label: string; error?: string };
export function AppField({ label, error, style, ...props }: Props) { return <View style={styles.group}><AppText variant="caption" style={styles.label}>{label}</AppText><TextInput {...props} accessibilityLabel={label} accessibilityHint={error} placeholderTextColor={palette.textSoft} selectionColor={palette.action} style={[styles.input, props.multiline && styles.multiline, error && styles.inputError, style]} />{error && <AppText variant="caption" style={styles.error}>{error}</AppText>}</View>; }
const styles = StyleSheet.create({ group: { gap: spacing.sm }, label: { fontWeight: '700', color: palette.text }, input: { minHeight: 52, borderWidth: 1, borderColor: palette.borderStrong, borderRadius: radius.md, backgroundColor: palette.surface, color: palette.text, paddingHorizontal: spacing.lg, fontSize: 16 }, inputError: { borderColor: palette.destructive }, multiline: { minHeight: 112, paddingVertical: spacing.md, textAlignVertical: 'top' }, error: { color: palette.destructive, fontWeight: '600' } });
